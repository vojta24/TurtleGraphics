<UserControl x:Class="TurtleGraphics.SaveDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TurtleGraphics"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<TextBox Grid.Row="0" Grid.ColumnSpan="2" Text="{Binding SaveFileName, UpdateSourceTrigger=PropertyChanged}"/>
		<Button Grid.Row="1" Content="Cancel" Command="{Binding CancelCommand}"/>
		<Button Grid.Row="1" Grid.Column="1" Content="Save" Command="{Binding SaveCommand}"/>
	</Grid>
</UserControl>
